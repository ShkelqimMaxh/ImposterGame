## Packages
framer-motion | Smooth animations for card reveals and transitions
canvas-confetti | For celebration effects when game ends
lucide-react | Icons (already in base but ensuring version)

## Notes
WebSocket connection should listen on `location.protocol // location.host /ws`
The app is mobile-first, so touch targets should be large (min 44px)
Imposter role needs to be kept secret - tap/hold interactions
